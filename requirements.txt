# Runtime dependencies for the modelling pipeline.
#
# Install with:  pip install -r requirements.txt
# Or install the package itself:  pip install -e .
#
# Note: TensorFlow/Keras is deliberately absent. At 43k rows and ~20 features
# scikit-learn's gradient boosting outperforms the old 32->16 MLP, trains in
# seconds, and gives calibrated probabilities without the TF dependency stack.

pandas>=2.2
numpy>=1.26
scikit-learn>=1.5
joblib>=1.4
matplotlib>=3.8

# Data collection only (Phase 6) -- install with: pip install -e ".[scrape]"
# selenium>=4.20
# rapidfuzz>=3.9

# Development -- install with: pip install -e ".[dev]"
# pytest>=8
# ruff>=0.6
